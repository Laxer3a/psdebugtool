#ifndef HELPER_STR_H
#define HELPER_STR_H

#include "myTypes.h"

u8* found        (u8* start, u8* sub);
u64 ValueHexAfter(u8* start, u8* pattern);
u64 ValueIntAfter(u8* start, u8* pattern);


#endif // HELPER_STR_H